containers:
- name: nginx-hello
  image: nginxdemos/hello:latest
  resources:
    requests:
      cpu: 1
      memoryInGB: 1.5
  ports:
  - port: 80
    protocol: TCP

osType: Linux
ipAddress:
  type: Public
  ports:
  - protocol: TCP
    port: 80
restartPolicy: Always